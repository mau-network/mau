# Mau GUI Environment Variables

# Development Bootstrap Peer (Node.js)
# These are automatically configured when you run `npm run dev`
# The bootstrap server is a Node.js peer using the TypeScript library
VITE_DEV_PEER_FINGERPRINT=
VITE_DEV_PEER_WS_ADDRESS=localhost:8444
