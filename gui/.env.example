# Mau GUI Environment Variables

# Development Bootstrap Peer
# Run `bun run dev:peer` to start the dev server and get the fingerprint
# Copy the fingerprint from the dev server output and paste it here
VITE_DEV_PEER_FINGERPRINT=
VITE_DEV_PEER_ADDRESS=localhost:8081
